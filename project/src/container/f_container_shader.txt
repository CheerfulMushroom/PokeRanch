#version 330 core

in vec2 TexCoord;

out vec4 color;

uniform sampler2D texture_pepe;
uniform sampler2D texture_box;

void main() {
    color = mix(texture(texture_box, TexCoord), texture(texture_pepe, TexCoord), 0.2);
};
